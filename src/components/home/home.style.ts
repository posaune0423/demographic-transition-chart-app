import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  main: {
    color: 'inherit',
    backgroundColor: 'inherit',
    paddingTop: '80px',
    paddingBottom: '110px'
  },
  title: {
    paddingLeft: '1rem'
  }
}));
